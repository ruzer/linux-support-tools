# Support Tools

Installed command:

```powershell
pc-audit
```

It writes a hardware/software report to:

```text
%USERPROFILE%\Desktop\pc-audit
```

Use it after installing a fresh laptop to capture CPU, RAM, GPU, disks, network,
Windows version, battery and key tool availability.

